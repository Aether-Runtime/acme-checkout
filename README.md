# acme-checkout

Checkout service and storefront for the Acme demo shop. Express API plus a Vite and React
storefront, all in TypeScript.

Scaffold only for now; see `package.json` for the available scripts.
