{
  "setup": "npm ci --no-audit --no-fund || npm ci --no-audit --no-fund || npm install --no-audit --no-fund",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:api"
    },
    {
      "name": "web",
      "command": "npm run dev:web"
    }
  ],
  "setup_on_boot": "npx playwright install chromium || true"
}
